name: JI podcast summarizer

on:
  # Ping via cron-job.org (workflow_dispatch API) for ~2-min cadence.
  workflow_dispatch:
  # Best-effort fallback if the pinger stops.
  schedule:
    - cron: "*/10 * * * *"

# poll.py commits watcher_state.json back to the repo.
permissions:
  contents: write

concurrency:
  group: poll
  cancel-in-progress: false

jobs:
  poll:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install deps
        run: pip install -r requirements.txt
      - name: Poll and summarize
        run: python3 poll.py
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # YouTube transcripts route through ClipMaker running on the Mac
          # (residential IP, bypasses YouTube's cloud-IP blocking).
          CLIPMAKER_URL: ${{ secrets.CLIPMAKER_URL }}
          CLIPMAKER_AUTH_TOKEN: ${{ secrets.CLIPMAKER_AUTH_TOKEN }}
      - name: Commit updated state
        run: |
          git config user.name "podcast-summarizer-bot"
          git config user.email "bot@users.noreply.github.com"
          git add watcher_state.json
          if git diff --cached --quiet; then
            echo "no state changes"
          else
            git commit -m "Update watcher state"
            git push
          fi
      - name: Alert Slack if the run failed
        if: failure()
        run: |
          curl -sS -X POST https://slack.com/api/chat.postMessage \
            -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
            -H 'Content-Type: application/json; charset=utf-8' \
            --data "{\"channel\":\"$SLACK_CHANNEL_ID\",\"text\":\":rotating_light: *YouTube summarizer run FAILED.* Check the log: $RUN_URL\"}" || true
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
